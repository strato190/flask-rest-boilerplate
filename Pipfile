[[source]]
url = "https://pypi.python.org/simple"
verify_ssl = true
name = "pypi"

[packages]
flask = ">=1.0"
flask-cors = ">=3.0"
connexion = {version = ">=2.2",extras = ["swagger-ui"]}
"boto3" = ">=1.9"
flask-env = ">=2.0"
flask-sqlalchemy = ">=2.3"
flask-migrate = ">=2.3"
"psycopg2-binary" = ">=2.7"
flask-marshmallow = ">=0.9"
sqlalchemy-utils = ">=0.33"
marshmallow-sqlalchemy = ">=0.15"
prometheus-client = ">=0.5"
sqlalchemy = "*"

[dev-packages]
pylint = ">=2.2"
pre-commit = ">=1.14"
pytest-flask = ">=0.14"
factory-boy = ">=2.11"

[requires]
python_version = "3.7"
